import React from 'react';
import styled from 'styled-components';

const UseCallbackTest = (tryHook: boolean) => {
  return <Container></Container>;
};

const Container = styled.div``;

export default UseCallbackTest;
